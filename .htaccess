RewriteEngine On

RewriteCond %{HTTP_HOST} ^(localhost|127\.0\.0\.1)$ [NC,OR]
RewriteCond %{SERVER_PORT} =8080
RewriteRule ^ - [L]

RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]
